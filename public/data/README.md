# Compendium SRD (JSON statique)

Ce dossier contiendra le compendium D&D 3.5 au format SRD (OGL 1.0a),
précaché par le service worker (voir `workbox.globPatterns` dans
`vite.config.ts`). Vide pour l'instant.
