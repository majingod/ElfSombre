# Composants UI

Couche présentation : composants fonction + hooks personnalisés
(`useCharacterCalculations`, `useSpellSlots`...). La logique de règles ne
vit jamais ici — elle appelle `src/engine/`.

Vide pour l'instant.
